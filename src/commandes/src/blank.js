function cmd(option) {
  // programme
}

function info() {
  const info = {
    name: 'here [name of the commande]',
    description: 'here [description of the commande]',
  };

  return info;
}

module.exports = { cmd, info };
